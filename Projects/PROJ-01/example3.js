
/*
At the top level of programs and functions, let, 
unlike var, does not create a property on the global object.

This is why var is bad. Conversely, it does create a property on the global scope. 
Thus creating a memory leak
*/ 
var a = 1;
var b = 1;
var c = 1;







const element = document.getElementById('button');
element.addEventListener('click', onClick)







setInterval(() => {
    // referencing objects...
    // that will never get collected by garbage collection
})





















